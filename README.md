# МЭШ — автозадание ДЗ по дню

Расширение для Chrome, автоматизирующее рутинную задачу в МЭШ (school.mos.ru): проставление домашнего задания «Не задано.» для уроков выбранного дня, у которых оно ещё не выставлено.

## Установка

1. Скачайте или склонируйте этот репозиторий.
2. Откройте `chrome://extensions`.
3. Включите «Режим разработчика» (в правом верхнем углу).
4. Нажмите «Загрузить распакованное расширение» и выберите папку репозитория.

## Использование

1. Откройте страницу расписания уроков в личном кабинете учителя МЭШ (`school.mos.ru/teacher/account/schedule`).
2. Нажмите на иконку расширения.
3. Выберите нужный день текущей недели.
4. Расширение пройдётся по урокам этого дня и создаст задание «Не задано.» для тех, у которых домашнее задание ещё отсутствует.

## Файлы

- `manifest.json` — манифест расширения (Manifest V3).
- `content.js` — основная логика: поиск уроков без ДЗ и создание задания.
- `inject.js` — скрипт, внедряемый в контекст страницы (`world: MAIN`).
- `background.js` — service worker.
- `popup.html` / `popup.js` — интерфейс всплывающего окна расширения.

## Какие записи в расписании обрабатываются

Расширение создаёт «Не задано.» только для обычных уроков — тех, у которых во всплывающей подсказке в расписании ДВЕ кнопки: «К журналу» и «К уроку». Только у таких уроков вообще есть форма «Домашнее задание отсутствует».

Два других вида записей в расписании МЭШ расширение сознательно пропускает, но при этом видит и честно показывает их количество в попапе:

- **Внеурочная деятельность** (бейдж «ВН», например «Разговоры о важном», «Спортивные игры») — в подсказке только одна кнопка «К журналу», кнопки «К уроку» нет вообще.
- **Доп. образование / секции** (бейдж «доп», например кружки, ШСК) — отдельная система, без статуса ДЗ и обычной формы урока.

Это работает автоматически: `inject.js` слушает три разных API-эндпоинта расписания (обычные уроки, внеурочная деятельность, доп. образование) и в очередь на создание ДЗ попадают только уроки из первого. Так как расширение получает данные напрямую из ответов сервера, а не через прокрутку страницы, оно не зависит от того, что видно на экране, и не может «забыть» пролистать день до конца — в отличие от ручной работы.

## Важно

Дата проверки задания при создании подставляется автоматически самим МЭШ (обычно — следующее занятие этого класса), так как сайт не позволяет выбрать дату проверки раньше «сегодня + 2 дня» ни при каких условиях. Это ограничение самого сайта, а не расширения.
